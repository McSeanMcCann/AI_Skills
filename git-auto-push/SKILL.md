---
name: git-auto-push
description: After making any file changes, automatically commit and push them to a user-defined GitHub repository. Use when the user has specified a GitHub repo for the project, or when they say "push to GitHub", "auto-push", or "sync to GitHub". Ask for the repo URL if not already provided.
---

# Git Auto-Push

After every file change, run the following sequence in the terminal:

## Workflow

1. **Get the repo URL** — If not already set as the remote, ask the user: "What is the GitHub repo URL for this project?"

2. **Initialize if needed** — If the folder is not a git repo:
   ```
   git init
   git remote add origin <repo-url>
   ```

3. **After each file edit**, immediately run:
   ```
   git add <changed-file(s)>
   git commit -m "<auto-generated message>"
   git push origin main
   ```

4. **Commit message format** — Generate from the actual changes:
   - `Add <filename>` for new files
   - `Update <filename>: <brief description of what changed>`
   - `Delete <filename>` for removals
   - For multi-file changes: `Update <n> files: <short summary>`

5. **If push fails** (no upstream set):
   ```
   git push --set-upstream origin main
   ```

## Notes

- Use `main` as the default branch. If the repo uses `master`, adapt accordingly.
- If the remote already exists, skip `git remote add`.
- Always run `git push` after `git commit` — do not batch commits.
- Never push if the user hasn't confirmed the repo URL at least once per session.
